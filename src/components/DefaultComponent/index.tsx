// src/components/DefaultComponent.tsx
import React from "react";

const DefaultComponent: React.FC = () => {
  return (
    <div>
      This is the default component. The requested component could not be
      loaded.
    </div>
  );
};

export default DefaultComponent;
